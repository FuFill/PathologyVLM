--extra-index-url https://download.pytorch.org/whl/cu129
torch==2.8.0
torchvision==0.23.0
clearml==1.16.4
transformers==4.37.2
tokenizers==0.15.2
accelerate==0.27.2
safetensors==0.4.2
sentencepiece==0.2.0
protobuf==4.25.3
bitsandbytes==0.43.1
huggingface_hub==0.20.3
datasets==2.16.1
pillow==10.3.0
numpy==1.26.4
pandas==2.2.2
tqdm==4.66.4
pyyaml==6.0.1
openslide-python==1.3.1
einops==0.7.0
shortuuid==1.0.11
# NOTE: The `llava` package (quilt-llava fork) is intentionally NOT listed here.
# Its setup.py pins torch==2.0.1, which conflicts with our torch==2.8.0+cu129
# pin and breaks `pip install -r requirements.txt` on the agent. Our inference
# code loads `wisdomik/Quilt-Llava-v1.5-7b` directly via
# `transformers.LlavaForConditionalGeneration`, which does not require the
# `llava` package at runtime, so we simply drop it.
